#run in /bin/bash
docker run -it (imagename) /bin/bash

#edited
#run this line to start spark (pyspark to run spark using python code)
./bin/pyspark

#to start container again after stopping
docker start -i (containername)
